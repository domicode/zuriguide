import { AgmLocationMarkerDirective } from './agm-location-marker.directive';

describe('AgmLocationMarkerDirective', () => {
  it('should create an instance', () => {
    const directive = new AgmLocationMarkerDirective();
    expect(directive).toBeTruthy();
  });
});
